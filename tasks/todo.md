# Tasks: Porting The Culinary Gallery (Onyx Earth) to Next.js

## 1. Global Setup
- [x] Initialize global CSS with Tailwind v4 theme configuration (from `chef_s_homepage/code.html`)
- [x] Configure `app/layout.tsx` to include `Noto Serif` and `Manrope` Google Fonts
- [x] Implement global utility classes for typography (`display-lg`, `headline-md`)

## 2. Page Implementations
- [x] Port `chef_s_homepage` to Next.js `/` page
- [x] Port `about_the_chef` to Next.js `/about` page
- [x] Port `booking_services` to Next.js `/booking` page
- [x] Port `culinary_gallery` to Next.js `/gallery` page
- [x] Port `recipe_journal` to Next.js `/journal` page

## 3. Verification
- [x] Run Next.js dev server and verify no console/build errors
- [x] Take screenshots using browser automation to verify design fidelity matches original HTML
